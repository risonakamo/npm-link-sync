{
  "name": "npm-link-sync",
  "version": "1.0.0",
  "scripts": {
    "buildrun": "tsc && node build/npm-link-sync",
    "build": "tsc"
  },
  "bin": {
    "npm-link-sync": "build/npm-link-sync.js"
  },
  "devDependencies": {
    "@types/node": "^14.0.27",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "execa": "^4.0.3",
    "lodash.intersection": "^4.4.0"
  }
}
